import { BusinessInfoMain } from "../../../widgets";
import { FormContainer, FormWrapper } from "../styles";

export const BusinessInfo = () => {
  return (
    <>
      <FormContainer>
        <FormWrapper>
          <BusinessInfoMain />
        </FormWrapper>
      </FormContainer>
    </>
  );
};
