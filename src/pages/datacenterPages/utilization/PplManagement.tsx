import { PplManagementMain } from "../../../widgets";
import { FormContainer, FormWrapper } from "../styles";

export const PplManagement = () => {
  return (
    <FormContainer>
      <FormWrapper>
        <PplManagementMain />
      </FormWrapper>
    </FormContainer>
  );
};
